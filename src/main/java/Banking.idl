module BankingApp{

    exception InsufficientBalance{
    string message;
    };

    interface Account{
        double getBalance(in string accNo);
        void deposit(in string accNo,in double amount);
        void withdraw(in string accNo,in double amount) raises (InsufficientBalance);
    };
};